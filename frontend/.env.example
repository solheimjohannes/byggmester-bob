# Frontend environment variables
# Copy to .env.local and fill in values

# API base URL — leave empty in dev so the Vite proxy (vite.config.ts server.proxy)
# routes /api/* and /health to the backend. This avoids cross-origin issues both
# locally and in Codespaces. Set a full URL only if you disable the proxy.
VITE_API_URL=
